import React from 'react';
import {formatPhone, isUnique} from "./Util";

describe('Test Util', () => {

	it('should format phone number', () => {
		let phone = formatPhone();

	});

	it('should create a Contact', function () {

	});

	it('should determine if a contact is unique', function () {
		let unique = isUnique();

	});
});
